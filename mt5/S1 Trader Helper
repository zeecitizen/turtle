This is a classic algorithmic trading reality check. I can see exactly why Claude’s backtest is giving you a 48% win rate, while your manual trading on February 11 felt like a 94% win rate.

The strategy *concept* you traded manually and the strategy *code* you wrote are actually doing two completely different things. You are trading like a high-frequency tape reader, but your EA is coded to trade like a slow, rigid swing trader.

Here is exactly where the EA’s code diverges from your manual execution on that Trade History Report:

### 1. The Entry Delay (Intra-candle vs. Candle Close)

When you trade manually, you are likely watching the live price action. The moment the live price sweeps the UHV red candle's high, you click "Buy."

Look at your EA’s `OnTick()` function:

```mql5
   datetime cur_m5 = iTime(_Symbol, InpTimeframe, 0);
   if (cur_m5 != g_last_m5_time && g_last_m5_time != 0) {
      if (!TryS1BuySignal()) TryS1SellSignal();
   }

```

Your EA is hardcoded to wait until the entire 5-minute candle **closes**. By the time a 5-minute breakout candle officially closes above the UHV high, the explosive momentum of the breakout is usually over. The EA ends up buying the absolute top of the M5 candle right before the market pulls back, immediately putting the trade in drawdown.

### 2. Time in Market (Seconds vs. Hours)

Looking at your trade history for Mohammad Zeeshan on Blueberry Markets, you are an aggressive scalper.

* At 16:58:03, you manually opened three buy positions and closed them just 24 seconds later.


* At 19:08:59, you opened four sell positions and closed them in exactly 9 seconds.


* At 19:42:26, you held a sell for 14 seconds to grab a quick profit.



Your EA is completely blind to these quick micro-moves. It uses a static take profit of 7.5 points (`InpTPPoints = 7.5`) and a rigid stop loss. If the market spikes in your favor by 5 points in 10 seconds and then reverses, your manual brain clicks "Close" and takes the money. The EA just sits there waiting for 7.5, misses it, and rides the trade all the way down to the stop loss.

### 3. Trade Stacking

Your manual history shows you heavily stacking positions. At 18:41:50, you fired four consecutive 0.10 lot buy trades within two seconds of each other.

Your EA specifically forbids this. The `ClusterBlocked(+1)` function and the `InpMaxOneSameDir = true` input ensure the EA will only ever fire one bullet at a time. It cannot multiply a small winning move by stacking lots the way you did manually.

### The Bottom Line

Claude's 48% assessment is mathematically correct for the code written in `S1Trader.mq5`. The EA enters late, holds too long, and ignores the intra-candle momentum that you naturally react to.

If you want the EA to replicate your 94% manual win rate on this UHV setup, we have to rewrite it to execute on live ticks (crossing the UHV high instantly) rather than waiting for M5 candle closes. Would you like me to rewrite the trigger logic so it enters the exact millisecond the breakout level is breached?


To make the EA trade exactly like you do manually, we have to rip out the "wait for candle close" constraint. We need a live, intra-candle trigger that fires the absolute millisecond the `Ask` or `Bid` price crosses the Ultra High Volume (UHV) candle's high/low.

To do this safely without the EA accidentally firing 100 trades in a single second while the price hovers on the breakout line, we will track the **timestamp of the UHV candle**. Once a specific UHV breakout is traded, the EA locks it out until a *new* UHV setup forms.

Here is the exact code to replace in `S1Trader.mq5` to weaponize it for live ticks.

### Step 1: Update the State Variables

Replace your existing `g_last_m5_time` and `g_last_signal_t` with these specific setup trackers.

**Find this in your code:**

```mql5
datetime g_last_m5_time = 0;
datetime g_last_signal_t = 0;

```

**Replace with this:**

```mql5
// Tracks the exact UHV candle we already traded so we don't double-fire
datetime g_last_uhv_buy_t = 0;
datetime g_last_uhv_sell_t = 0;

```

### Step 2: Replace the Buy and Sell Functions

Delete your current `TryS1BuySignal()` and `TryS1SellSignal()` functions entirely. Replace them with these live-tick versions.

Notice how they now check the live `ask` and `bid` prices against the UHV high/low, instead of checking the close of `shift 1`.

```mql5
//── S1 BUY LIVE TICK TRIGGER ─────────────────────────────────────────
bool TryS1BuySignalLive(double ask, double bid) {
   if (!InpDoBuys) return false;
   if (DailyLossHalted()) return false;
   if (InpERMin > 0 && EfficiencyRatio() < InpERMin) return false;

   if (!IsUptrendM5(1)) return false;

   // 1. Walk back collecting retracement reds in last InpRetraceLookback closed bars
   int reds[15];
   int reds_count = 0;
   int max_red_shift = 1;
   for (int j = 1; j <= InpRetraceLookback; j++) {
      if (iClose(_Symbol, InpTimeframe, j) < iOpen(_Symbol, InpTimeframe, j)) {
         if (reds_count < 15) {
            reds[reds_count++] = j;
            max_red_shift = j;
         }
      }
   }
   if (reds_count == 0) return false;

   // 2. Find UHV red
   int uhv_shift = reds[0];
   long uhv_vol = iVolume(_Symbol, InpTimeframe, uhv_shift);
   for (int r = 1; r < reds_count; r++) {
      long v = iVolume(_Symbol, InpTimeframe, reds[r]);
      if (v > uhv_vol) {
         uhv_vol = v;
         uhv_shift = reds[r];
      }
   }
   double uhv_h = iHigh(_Symbol, InpTimeframe, uhv_shift);
   double uhv_l = iLow (_Symbol, InpTimeframe, uhv_shift);
   datetime uhv_t = iTime(_Symbol, InpTimeframe, uhv_shift);

   // ANTI-SPAM: If we already bought this specific UHV breakout, skip it
   if (uhv_t == g_last_uhv_buy_t) return false;

   if (!IsBigSpreadClimax(uhv_shift)) return false;

   // 3. Sweep check: closed bars OR current live floating bar
   bool swept = false;
   for (int k = uhv_shift - 1; k >= 1; k--) {
      if (iLow(_Symbol, InpTimeframe, k) < uhv_l) { swept = true; break; }
   }
   if (!swept && iLow(_Symbol, InpTimeframe, 0) < uhv_l) swept = true;
   if (!swept) return false;

   if (InpRequireH1Fvg && !H1FvgTappedDuringRetracement(max_red_shift)) return false;

   // 4. LIVE TRIGGER: Has the current Ask price broken above the UHV high?
   if (ask <= uhv_h) return false;

   // 5. Fire instantly
   double sl = uhv_l - InpSLBufferPts;
   double tp = ask + InpTPPoints;

   g_signals_today++;
   Log(StringFormat("S1 BUY LIVE TRIGGER — entry=%.2f sl=%.2f tp=%.2f uhv_shift=%d (vol=%d)",
                     ask, sl, tp, uhv_shift, (int)uhv_vol));

   if (ClusterBlocked(+1)) return false;

   if (!g_trade.Buy(InpLots, _Symbol, ask, sl, tp, "S1_buy_live")) {
      Log(StringFormat("[ERR] Buy failed: ret=%d %s", g_trade.ResultRetcode(), g_trade.ResultRetcodeDescription()));
      return false;
   }
   
   g_entries_today++;
   g_last_uhv_buy_t = uhv_t; // Lock out this setup until a new UHV forms
   ulong tkt = g_trade.ResultOrder();
   Log(StringFormat("[FILLED] ticket=%d", tkt));
   LogEntryCsv("buy", ask, sl, tp, tkt);
   return true;
}

//── S1 SELL LIVE TICK TRIGGER ────────────────────────────────────────
bool TryS1SellSignalLive(double bid, double ask) {
   if (!InpDoSells) return false;
   if (DailyLossHalted()) return false;
   if (InpERMin > 0 && EfficiencyRatio() < InpERMin) return false;

   if (!IsDowntrendM5(1)) return false;

   // 1. Walk back collecting retracement greens in last InpRetraceLookback closed bars
   int greens[15];
   int greens_count = 0;
   int max_green_shift = 1;
   for (int j = 1; j <= InpRetraceLookback; j++) {
      if (iClose(_Symbol, InpTimeframe, j) > iOpen(_Symbol, InpTimeframe, j)) {
         if (greens_count < 15) {
            greens[greens_count++] = j;
            max_green_shift = j;
         }
      }
   }
   if (greens_count == 0) return false;

   // 2. Find UHV green
   int uhv_shift = greens[0];
   long uhv_vol = iVolume(_Symbol, InpTimeframe, uhv_shift);
   for (int r = 1; r < greens_count; r++) {
      long v = iVolume(_Symbol, InpTimeframe, greens[r]);
      if (v > uhv_vol) {
         uhv_vol = v;
         uhv_shift = greens[r];
      }
   }
   double uhv_h = iHigh(_Symbol, InpTimeframe, uhv_shift);
   double uhv_l = iLow (_Symbol, InpTimeframe, uhv_shift);
   datetime uhv_t = iTime(_Symbol, InpTimeframe, uhv_shift);

   // ANTI-SPAM: If we already sold this specific UHV breakout, skip it
   if (uhv_t == g_last_uhv_sell_t) return false;

   if (!IsBigSpreadClimax(uhv_shift)) return false;

   // 3. Sweep check: closed bars OR current live floating bar
   bool swept = false;
   for (int k = uhv_shift - 1; k >= 1; k--) {
      if (iHigh(_Symbol, InpTimeframe, k) > uhv_h) { swept = true; break; }
   }
   if (!swept && iHigh(_Symbol, InpTimeframe, 0) > uhv_h) swept = true;
   if (!swept) return false;

   if (InpRequireH1Fvg && !H1BearFvgTappedDuringRetracement(max_green_shift)) return false;

   // 4. LIVE TRIGGER: Has the current Bid price broken below the UHV low?
   if (bid >= uhv_l) return false;

   // 5. Fire instantly
   double sl = uhv_h + InpSLBufferPts;
   double tp = bid - InpTPPoints;

   g_signals_today++;
   Log(StringFormat("S1 SELL LIVE TRIGGER — entry=%.2f sl=%.2f tp=%.2f uhv_shift=%d (vol=%d)",
                     bid, sl, tp, uhv_shift, (int)uhv_vol));

   if (ClusterBlocked(-1)) return false;

   if (!g_trade.Sell(InpLots, _Symbol, bid, sl, tp, "S1_sell_live")) {
      Log(StringFormat("[ERR] Sell failed: ret=%d %s", g_trade.ResultRetcode(), g_trade.ResultRetcodeDescription()));
      return false;
   }
   
   g_entries_today++;
   g_last_uhv_sell_t = uhv_t; // Lock out this setup until a new UHV forms
   ulong tkt = g_trade.ResultOrder();
   Log(StringFormat("[FILLED] ticket=%d", tkt));
   LogEntryCsv("sell", bid, sl, tp, tkt);
   return true;
}

```

### Step 3: Unleash the OnTick() Loop

Finally, replace your `OnTick()` function at the very bottom of the script. We remove the `cur_m5` delay entirely. The EA will now check for the breakout logic on *every single tick* the broker sends.

```mql5
void OnTick() {
   IsNewDay();
   
   double bid = SymbolInfoDouble(_Symbol, SYMBOL_BID);
   double ask = SymbolInfoDouble(_Symbol, SYMBOL_ASK);
   if (bid <= 0 || ask <= 0) return;

   // Live trigger scanning on every tick (no waiting for candle close)
   if (!TryS1BuySignalLive(ask, bid)) {
      TryS1SellSignalLive(bid, ask);
   }

   ManageOpenPositions();   // 2R Free Roll
   CheckGrabCommand();      // one-tap GRAB
   WriteHeartbeat();
}

```